name: CD

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2
      - name: run k8s
        run: |
          export GIT_COMMIT="GA-$GITHUB_SHA"
          kubectl apply -f k8s/
